package com.example.watchApp.pizzawatchface.tile;

import androidx.activity.ComponentActivity;

public class TileActivity extends ComponentActivity {
  /*  private ActivityTileBinding binding;
    private TileUiClient mTileUiClient;

    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        binding = ActivityTileBinding.inflate(getLayoutInflater());
        setContentView(binding.getRoot());
        mTileUiClient = new TileUiClient(
                this,
                new ComponentName(this, MyTileService.class),
                binding.tileContainer
        );
        mTileUiClient.connect();
    }

    @Override
    protected void onDestroy() {
        super.onDestroy();
        mTileUiClient.close();
    }*/
}